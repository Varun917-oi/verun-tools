import type { MetadataRoute } from "next"; import { categories, tools } from "@/data/registry"; const base=process.env.NEXT_PUBLIC_SITE_URL||"https://veruntools.com"; export default function sitemap():MetadataRoute.Sitemap{return ["","/tools","/about","/contact","/privacy","/terms","/cookie-policy","/disclaimer"].map(path=>({url:`${base}${path}`,lastModified:new Date()})).concat(categories.map(c=>({url:`${base}/category/${c.slug}`,lastModified:new Date()})),tools.map(t=>({url:`${base}/tools/${t.slug}`,lastModified:new Date()})));}
