import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import ToolClient from "@/components/ToolClient"; import ToolCard from "@/components/ToolCard"; import { AdSlot, Footer, Header } from "@/components/SiteShell"; import { getTool, tools } from "@/data/registry";
export function generateStaticParams(){return tools.map(t=>({slug:t.slug}));} export async function generateMetadata({params}:PageProps<"/tools/[slug]">):Promise<Metadata>{const tool=getTool((await params).slug);return tool?{title:tool.seoTitle,description:tool.seoDescription,keywords:tool.keywords,alternates:{canonical:`/tools/${tool.slug}`},openGraph:{title:tool.seoTitle,description:tool.seoDescription}}:{title:"Tool not found"};} export default async function Page({params}:PageProps<"/tools/[slug]">){const tool=getTool((await params).slug);if(!tool)notFound();const Icon=tool.icon;const related=tools.filter(t=>t.slug!==tool.slug&&(t.category===tool.category||t.popular)).slice(0,3);const jsonLd={"@context":"https://schema.org","@type":"WebApplication",name:tool.name,description:tool.description,applicationCategory:"UtilitiesApplication",operatingSystem:"Any",offers:{"@type":"Offer",price:"0",priceCurrency:"USD"}};return <><Header/><main className="mx-auto min-h-screen max-w-5xl px-6 py-10"><Link href="/tools" className="text-sm text-white/45 hover:text-white">← Back to tools</Link><div className="mt-10 text-center"><Icon className="mx-auto text-white/70" size={32}/><p className="mt-4 text-xs uppercase tracking-[.2em] text-white/35">{tool.category} tool</p><h1 className="mt-2 text-4xl font-semibold tracking-tight sm:text-5xl">{tool.name}</h1><p className="mx-auto mt-4 max-w-2xl text-white/50">{tool.description}</p></div><section className="mt-10 rounded-3xl border border-white/10 bg-white/[.025] p-5 sm:p-8"><ToolClient tool={{ slug: tool.slug, kind: tool.kind }} /></section><p className="mt-4 text-center text-xs text-white/35">Processing stays in your browser where technically practical. Your input is not stored by VERUN TOOLS.</p><AdSlot/><section><h2 className="text-2xl font-semibold">How to use</h2><p className="mt-3 max-w-3xl leading-7 text-white/50">Enter your content or choose your file, adjust the available settings, and use the clearly labelled action to generate your result. Download and copy actions create a result locally in your browser.</p></section><section className="mt-14"><h2 className="text-2xl font-semibold">Related tools</h2><div className="mt-5 grid gap-4 sm:grid-cols-3">{related.map(t=><ToolCard key={t.slug} tool={t}/>)}</div></section></main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><Footer/></>}
