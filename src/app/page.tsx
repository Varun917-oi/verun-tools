import Link from "next/link";
import { ArrowRight, ShieldCheck, Zap } from "lucide-react";
import { categories, tools } from "@/data/registry";
import ToolCard from "@/components/ToolCard";
import SearchBar from "@/components/SearchBar";
import { Footer, Header } from "@/components/SiteShell";
export default function Home(){const popular=tools.filter(t=>t.popular).slice(0,6);return <><Header/><main><section className="relative overflow-hidden border-b border-white/10"><div className="absolute left-1/2 top-0 -z-10 h-96 w-[44rem] -translate-x-1/2 rounded-full bg-violet-500/10 blur-[130px]"/><div className="mx-auto max-w-5xl px-6 pb-24 pt-24 text-center sm:pt-32"><p className="mx-auto mb-6 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[.03] px-4 py-2 text-sm text-white/55"><Zap size={15}/>Simple tools. Powerful results.</p><h1 className="text-5xl font-semibold tracking-tight sm:text-6xl md:text-7xl">Your tools.<br/><span className="text-white/40">Simplified.</span></h1><p className="mx-auto mt-6 max-w-2xl text-lg leading-7 text-white/50">Free, fast, privacy-conscious online tools for everyday work—without accounts or needless uploads.</p><div className="mx-auto mt-10 max-w-2xl"><SearchBar large/></div></div></section><section className="mx-auto max-w-7xl px-6 py-20"><div className="mb-10 flex items-end justify-between"><div><p className="text-xs uppercase tracking-[.2em] text-white/35">Start here</p><h2 className="mt-2 text-3xl font-semibold">Popular tools</h2></div><Link href="/tools" className="flex items-center gap-2 text-sm text-white/50 hover:text-white">View all <ArrowRight size={16}/></Link></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{popular.map(t=><ToolCard key={t.slug} tool={t}/>)}</div></section><section className="border-y border-white/10 bg-white/[.015]"><div className="mx-auto max-w-7xl px-6 py-20"><p className="text-xs uppercase tracking-[.2em] text-white/35">Explore</p><h2 className="mt-2 text-3xl font-semibold">Everything you need</h2><div className="mt-9 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{categories.map(c=><Link href={`/category/${c.slug}`} key={c.slug} className="rounded-xl border border-white/10 p-5 text-sm text-white/65 transition hover:border-white/25 hover:bg-white/[.04] hover:text-white">{c.name}</Link>)}</div></div></section><section className="mx-auto grid max-w-7xl gap-6 px-6 py-16 md:grid-cols-2"><div className="rounded-2xl border border-white/10 p-7"><ShieldCheck className="text-emerald-300"/><h2 className="mt-5 text-xl font-semibold">Your files stay with you</h2><p className="mt-2 text-sm leading-6 text-white/50">Image and PDF tools process selected files in your browser whenever possible. We do not build an upload queue or account around your work.</p></div><div className="rounded-2xl border border-white/10 p-7"><Zap className="text-violet-300"/><h2 className="mt-5 text-xl font-semibold">Built for quick work</h2><p className="mt-2 text-sm leading-6 text-white/50">Open a tool, get the result, then move on. Every action has a clear purpose and works comfortably on small screens too.</p></div></section></main><Footer/></>}
